import 'package:flutter/material.dart';

class KickUi extends StatelessWidget {
  const KickUi({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('우리만의 킥 화면'));
  }
}
